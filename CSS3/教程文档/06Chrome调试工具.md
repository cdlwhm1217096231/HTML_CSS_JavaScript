#### 1.打开调试工具
- 打开Chrome浏览器，按下**F12键或右击页面空白处->检查**。
#### 2.使用调试工具
- **Ctrl+滚轮**可以放大开发者工具代码大小；
- 左边是HTML元素结构，右边是CSS样式；
- 右边CSS样式可以改变数值(左右箭头或者直接输入)和查看颜色
- **Ctrl+0**复原浏览器大小
- 如果点击元素，发现右侧没有样式引入，极有可能是类名或样式引入错误；
- 如果有样式，但是样式前面有**黄色叹号提示**，则是样式属性书写错误；